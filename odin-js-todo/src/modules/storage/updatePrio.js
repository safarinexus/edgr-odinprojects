export default function updatePrio(projectid, task) {
    /*const proj = localStorage.getItem(projectid);
    for (let i = 0; i < proj.store.length; i++) {
        const task = store[i];
        if (task.id === ) {}
    }
    */
}