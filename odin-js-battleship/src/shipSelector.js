const shipSelector = () => {
    for (let ships = 0; ships < 5; ships++) { 
        const orientation = Math.floor(Math.random() * 2);
        if (orientation === 0) {

        } else {
            
        }
    }
}

module.exports = shipSelector;